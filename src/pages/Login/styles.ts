import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  main: {
    flex: 1,
    padding: 40,
    justifyContent: 'center',
  },

  title: {
    color: "#fff",
    fontSize: 35,
    fontFamily: 'Roboto',
    fontWeight: 'bold',
    marginBottom: 24.84,
  },

  inputFields: {
    paddingVertical: 70,
  },
});