import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  input: {
    color: '#000',
    height: 60,
    backgroundColor: "#FFF",
    borderRadius: 10,
    paddingHorizontal: 12,
    marginVertical: 12,
    fontSize: 16,
  },
});